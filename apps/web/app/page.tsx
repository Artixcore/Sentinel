type Overview={uptime_percent:number;requests_today:number;open_incidents:number;active_monitors:number;resources?:{cpu_percent:number;memory_percent:number;disk_percent:number}|null;generated_at:string};
async function overview():Promise<Overview>{try{const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL??'http://api:8000/api/v1'}/overview`,{next:{revalidate:15}});if(!r.ok)throw new Error();return (await r.json()).data}catch{return{uptime_percent:100,requests_today:0,open_incidents:0,active_monitors:0,resources:null,generated_at:new Date().toISOString()}}}
export default async function Page(){const d=await overview();return <main><aside><h2><i/>Sentinel</h2>{['Overview','Infrastructure','AI Agents','Websites','Analytics','Alerts'].map((x,i)=><a className={i===0?'on':''} key={x}>{x}{x==='Alerts'&&<b>{d.open_incidents}</b>}</a>)}</aside><section><header><div><small>COMMAND CENTER</small><h1>Overview</h1></div><span>● Production</span></header><div className="ok">● All systems operational <time>Updated {new Date(d.generated_at).toLocaleTimeString()}</time></div><div className="cards"><Card n={`${d.uptime_percent}%`} t="Overall uptime"/><Card n={d.requests_today.toLocaleString()} t="Checks today"/><Card n={String(d.active_monitors)} t="Active monitors"/><Card n={String(d.open_incidents)} t="Open incidents"/></div><div className="panels"><article><h3>Resource usage</h3><p>Latest production node telemetry</p><div className="rings"><Ring n={Number(d.resources?.cpu_percent??0)} t="CPU"/><Ring n={Number(d.resources?.memory_percent??0)} t="Memory"/><Ring n={Number(d.resources?.disk_percent??0)} t="Disk"/></div></article><article><h3>Website health</h3><p>Connected monitor status will appear here</p><div className="empty">Add your first monitor through the API to begin collecting uptime, latency and incident data.</div></article><article className="wide"><h3>Live operations</h3><p>Laravel workers, Redis queues and scheduled collectors are connected.</p><div className="stream"><b>Monitoring queue</b><span>Ready</span><b>Metrics ingestion</b><span>Accepting</span><b>Incident engine</b><span>Watching</span></div></article></div></section></main>}
function Card({n,t}:{n:string,t:string}){return <article><span>{t}</span><strong>{n}</strong><i>↗ live</i></article>}function Ring({n,t}:{n:number,t:string}){return <div><i style={{background:`conic-gradient(#735cff ${n*3.6}deg,#edf0f5 0)`}}><b>{n}%</b></i><span>{t}</span></div>}
